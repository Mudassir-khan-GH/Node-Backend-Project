<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Verify Account | SnapFix</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>

  <body class="bg-[#f8fafc] min-h-screen flex flex-col">
    <!-- Navbar -->
    <nav class="bg-white border-b border-gray-200 px-6 py-3 flex justify-between items-center">
      <h1 class="text-2xl font-semibold text-blue-600 tracking-tight">SnapFix.</h1>
      <a href="/api/v1/user/profile" class="text-gray-600 hover:text-blue-600 font-medium transition">← Back</a>
    </nav>

    <!-- Verification Form -->
    <main class="flex flex-col items-center justify-center flex-grow px-4">
      <div class="bg-white w-full max-w-md rounded-3xl shadow-sm py-10 px-8 text-center">
        <h2 class="text-xl font-semibold text-gray-800 mb-2">Verify Your Account</h2>
        <p class="text-sm text-gray-500 mb-8">
          Please enter the 6-digit code sent to your email address at. <br>
            <h3 class="font-semibold text-gray-600 mb-[10px] mt-[-10px]"><%= user.email %></h3>
        </p>

        <form action="/api/v1/user/verify" method="POST" class="flex flex-col gap-5">
          <input
            type="text"
            name="verificationCode"
            maxlength="6"
            placeholder="Enter 6-digit code"
            class="w-full text-center tracking-widest text-lg font-medium py-3 border border-gray-300 rounded-xl focus:ring-2 focus:ring-blue-500 focus:outline-none"
            required
          />

          <button
            type="submit"
            class="bg-blue-600 text-white text-sm py-2.5 rounded-xl hover:bg-blue-700 transition"
          >
            Verify Account
          </button>

          <p class="text-xs text-gray-500 mt-2">
            Didn’t get the code?
            <a href="/api/v1/user/resend-code" class="text-blue-600 hover:underline">Resend</a>
          </p>
        </form>
      </div>
    </main>
  </body>
</html>
